# Railway Deployment Requirements - ULTRA MINIMAL for Fastest Builds
# Only includes packages that are actually imported by railway_start.py

# Core API Framework (Required)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Database (Required)
pymongo[srv]>=4.0.0

# Trading API (Required) 
alpaca-trade-api>=3.0.0
alpaca-py>=0.8.0

# Market Data (Required)
polygon-api-client>=1.12.0

# Data Processing (Core only)
pandas>=1.5.0,<2.1.0
numpy>=1.24.0,<2.0.0

# AI APIs (Required for ai_learning_engine)
openai>=1.0.0
anthropic>=0.3.0

# Utilities (Required)
requests>=2.31.0
python-dotenv>=1.0.0
pytz>=2023.3
loguru>=0.7.0
psutil>=5.9.0

# Core Python Build Tools
setuptools>=68.0.0,<81.0.0
wheel>=0.41.0
pip>=23.0.0
